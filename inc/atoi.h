#ifndef ATOI_H
#define ATOI_H

// perform computation of a^expoennt only valid for unsigned interger
unsigned int IntPow(unsigned int a , unsigned int exponent);
unsigned int atoi(char* str, int length);

#endif
